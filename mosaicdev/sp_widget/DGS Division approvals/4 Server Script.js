(function() {
  /* populate the 'data' object */
  /* e.g., data.table = $sp.getValue('table'); */

	data.first_active = true;
	data.title_first_button = gs.getMessage(options.title_first_button).toUpperCase();
	data.title_second_button = gs.getMessage(options.title_second_button).toUpperCase();
	data.active = false;
	
})();